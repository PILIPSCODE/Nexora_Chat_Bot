// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String  @id @default(uuid())
  firstName String  @db.VarChar(50)
  lastName  String? @db.VarChar(50)
  email     String  @unique @db.Text
  password  String? @db.VarChar(100)
  picture   String? @db.Text
  role      String  @default("user")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  agent           UserAgent[]
  bot             Bot[]
  authProvider    Account[]
  userOtp         UserOtp[]
  userIntegration UserIntegration[]
  userSubcription UserSubcribtion[]

  product                  Product[]
  WhatsaapBussinessAccount WhatsaapBussinessAccount[]

  @@map("User")
}

model Account {
  id                Int      @id @default(autoincrement())
  userId            String
  provider          String   @db.VarChar(50)
  providerAccountId String   @db.VarChar(100)
  accessToken       String?  @db.VarChar(255)
  refreshToken      String?  @db.VarChar(255)
  expires_at        Int?
  token_type        String?
  scope             String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onUpdate: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@map("Account")
}

model UserSubcribtion {
  id            String       @id @default(uuid())
  userId        String
  subcribtionId Int
  tokenRemain   BigInt
  startDate     DateTime
  endDate       DateTime
  user          User         @relation(fields: [userId], references: [id], onUpdate: Cascade)
  subcribtion   Subscribtion @relation(fields: [subcribtionId], references: [id], onUpdate: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("UserSubcribtion")
}

model Subscribtion {
  id               Int               @id @default(autoincrement())
  name             String
  price            Int
  initialToken     BigInt
  durationDays     Int
  userSubcribtions UserSubcribtion[]

  @@map("Subcribtion")
}

model WhatsappSession {
  id        String   @id
  data      String   @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("WhatsappSession")
}

model WhatsappKeys {
  id        String
  type      String
  value     String @db.Text
  sessionId String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@id([sessionId, id, type])
  @@map("WhatsappKeys")
}

model WhatsaapBussinessAccount {
  id          String        @id @default(uuid())
  accessToken String        @db.VarChar(255)
  userId      String
  facebookId  String
  email       String?       @db.VarChar(100)
  businessId  String        @db.VarChar(255)
  user        User          @relation(fields: [userId], references: [id], onUpdate: Cascade)
  numberPhone NumberPhone[]

  @@map("WhatsaapBussinessAccount")
}

model NumberPhone {
  id                         String                   @id @default(uuid())
  numberPhoneId              String
  whatsaapBussinessAccountId String
  WhatsaapBussinessAccount   WhatsaapBussinessAccount @relation(fields: [whatsaapBussinessAccountId], references: [id], onUpdate: Cascade)

  @@map("NumberPhone")
}

model UserOtp {
  id        Int      @id @default(autoincrement())
  userId    String
  otpCode   String   @db.VarChar(10)
  isUsed    Boolean  @default(false)
  expiresAt DateTime
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onUpdate: Cascade)

  @@index([userId])
  @@index([expiresAt])
  @@map("UserOTP")
}

model UserAgent {
  id            String   @id @default(uuid())
  userId        String
  name          String   @db.VarChar(100)
  agent         String   @db.VarChar(100)
  prompt        String?  @db.Text
  filePath      String   @db.Text
  productIds    String[]
  vectoreStatus String   @default("Processing")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User  @relation(fields: [userId], references: [id], onUpdate: Cascade)
  bot  Bot[]

  @@index([userId])
  @@map("UserAgent")
}

model Product {
  id          String   @id @default(uuid())
  userId      String
  categoryId  String
  name        String   @db.VarChar(100)
  description String   @db.Text
  price       Int
  stock       Int
  weight      Decimal? @db.Decimal(10, 2)
  image       String   @db.Text
  sku         String   @db.VarChar(50)
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user     User     @relation(fields: [userId], references: [id])
  category Category @relation(fields: [categoryId], references: [id])

  variantOptions  VariantOption[]
  productVariants ProductVariant[]
  orderItem       OrderItem[]

  @@index([userId])
  @@index([categoryId])
  @@map("Product")
}

model Category {
  id        String   @id @default(uuid())
  name      String   @db.VarChar(100)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  products Product[]

  @@map("Category")
}

model VariantOption {
  id        String @id @default(uuid())
  productId String
  name      String @db.VarChar(100)

  product Product        @relation(fields: [productId], references: [id], onDelete: Cascade)
  values  VariantValue[]

  @@map("VariantOption")
}

model VariantValue {
  id       String @id @default(uuid())
  optionId String
  value    String @db.VarChar(100)

  option               VariantOption         @relation(fields: [optionId], references: [id], onDelete: Cascade)
  productVariantValues ProductVariantValue[]

  @@map("VariantValue")
}

model ProductVariant {
  id        String  @id @default(uuid())
  productId String
  sku       String  @db.VarChar(255)
  stock     Int
  price     Int
  image     String? @db.VarChar(500)

  product Product               @relation(fields: [productId], references: [id], onDelete: Cascade)
  values  ProductVariantValue[]

  @@map("ProductVariant")
}

model ProductVariantValue {
  id        String @id @default(uuid())
  variantId String
  valueId   String

  variant ProductVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)
  value   VariantValue   @relation(fields: [valueId], references: [id], onDelete: Cascade)

  @@map("ProductVariantValue")
}

model Bot {
  id              String   @id @default(uuid())
  userId          String
  agentId         String
  botName         String   @db.VarChar(100)
  isActive        Boolean? @default(false)
  type            String   @db.VarChar(50)
  data            String?  @db.VarChar(700)
  numberPhoneWaba String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user  User      @relation(fields: [userId], references: [id], onUpdate: Cascade)
  agent UserAgent @relation(fields: [agentId], references: [id], onUpdate: Cascade)
  order Order[]

  @@index([userId])
  @@index([isActive])
  @@map("BOT")
}

model Conversation {
  id              String    @id @default(uuid())
  botId           String
  userId          String
  room            String    @db.VarChar(100)
  humanHandle     Boolean   @default(false)
  integrationType String    @db.VarChar(100)
  sender          String    @db.VarChar(225)
  messages        Message[]
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([botId])
  @@index([botId, createdAt])
  @@map("Conversation")
}

model Message {
  id             String   @id @default(uuid())
  conversationId String
  message        Json
  sentiment      String
  type           String   @db.VarChar(100)
  role           String
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  conversation Conversation @relation(fields: [conversationId], references: [id], onUpdate: Cascade, onDelete: Cascade)

  @@index([conversationId, createdAt])
  @@map("Message")
}

model Customer {
  id              String @id @default(uuid())
  fullname        String @db.VarChar(200)
  phone           String @db.VarChar(100)
  email           String @db.VarChar(100)
  address         String @db.VarChar(1000)
  status          String @db.VarChar(50)
  tag             String @db.VarChar(50)
  integrationType String @db.VarChar(50)

  customerNote CustomerNote[]
  order        Order[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("Customer")
}

model CustomerNote {
  id         Int    @id @default(autoincrement())
  customerId String
  content    String @db.VarChar(1000)

  createdAt DateTime @default(now())
  customer  Customer @relation(fields: [customerId], references: [id], onUpdate: Cascade)
  updatedAt DateTime @updatedAt

  @@map("CustomerNote")
}

model Order {
  id          String  @id @default(uuid())
  customerId  String
  botId       String
  status      String  @db.VarChar(50)
  totalAmount Decimal @db.Decimal(14, 2)

  createdAt DateTime @default(now())

  transaction Transaction[]
  orderitems  OrderItem[]
  customer    Customer      @relation(fields: [customerId], references: [id], onUpdate: Cascade)
  bot         Bot           @relation(fields: [botId], references: [id], onUpdate: Cascade)

  @@index([customerId])
  @@index([botId])
  @@index([status])
  @@map("Order")
}

model Transaction {
  id            String  @id @default(uuid())
  orderId       String
  amount        Decimal @db.Decimal(14, 2)
  status        String  @db.VarChar(50)
  transactionId String  @db.VarChar(225)

  createdAt DateTime @default(now())
  order     Order    @relation(fields: [orderId], references: [id], onUpdate: Cascade)

  @@index([orderId])
  @@index([transactionId])
  @@map("Transaction")
}

model OrderItem {
  id        String   @id @default(uuid())
  orderId   String
  productId String
  quantity  Int
  price     Int
  createdAt DateTime @default(now())

  order   Order   @relation(fields: [orderId], references: [id], onUpdate: Cascade)
  product Product @relation(fields: [productId], references: [id], onUpdate: Cascade)

  @@map("OrderItem")
}

model Integration {
  id   Int    @id @default(autoincrement())
  name String @db.VarChar(100)
  type String @db.VarChar(50)

  userIntegration UserIntegration[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("Integration")
}

model UserIntegration {
  id     String @id @default(uuid())
  userId String

  connectedAt   DateTime
  isconnected   Boolean
  provider      String   @db.VarChar(50)
  integrationId Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  integration         Integration?         @relation(fields: [integrationId], references: [id], onUpdate: Cascade)
  user                User?                @relation(fields: [userId], references: [id], onUpdate: Cascade)
  contentIntegrations ContentIntegration[]

  @@unique([userId, integrationId])
  @@index([userId])
  @@index([integrationId])
  @@map("UserIntegration")
}

model ContentIntegration {
  id                String          @id @default(uuid())
  userIntegrationId String
  userIntegration   UserIntegration @relation(fields: [userIntegrationId], references: [id], onUpdate: Cascade, onDelete: Cascade)
  type              String          @db.VarChar(100)
  configJson        Json

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userIntegrationId])
  @@index([userIntegrationId, type])
  @@map("ContentIntegration")
}

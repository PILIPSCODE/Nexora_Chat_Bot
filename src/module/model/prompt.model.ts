export class PromptApi {
  name: string;
  prompt: string;
  modelName?: string;
}

export class changePrompt {
  id: string;
  name: string;
  prompt: string;
  modelName?: string;
}
export class postPrompt {
  userId: string;
  name: string;
  prompt: string;
  modelName?: string;
}
